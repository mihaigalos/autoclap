[package]
name = "autoclap"
version = "0.3.5"
edition = "2021"
authors = ["Mihai Galos <mihaigalos@gmail.com>"]
description = "👏 Auto-propagate Cargo.toml infos (name, version, author, repo) into app."
categories = ["command-line-utilities", "filesystem"]
keywords = ["disk", "disk-usage"]
repository = "https://github.com/mihaigalos/autoclap"
license = "MIT"

[dependencies]
clap = { version = "4.0.13", features = ["string"]}
